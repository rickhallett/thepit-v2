# Plank 1 — The One-Page Story (Signal)

```signal
-- What I did
DEF project := two_phases(pilot + calibration) | solo | 350+_hrs | public

PHASE pilot(tspit) :=
  duration := 24_days | 847_commits | 278_SDs
  product  := adversarial_ai_eval_platform | full_stack | agents_under_governance
  observed := 18+_instances(output.valid & output.passed_checks & output.wrong)
  failure  := !hallucination | sycophantic_drift | epistemic_theatre | context_degradation
  built    := taxonomy(18_named_antipatterns) + layer_model(L0..L12, weights->human)
  pivot    := SD-130 | agent.performing_honesty & !honest_about_confidence
  instrument := human_judgment | "off before provable" | mental_health_background.transfers

PHASE calibration(noopit) :=
  purpose  := test(controls_work) | same_spec | compressed_governance | measured_from_commit_0
  artifacts := {
    signal_protocol  : 4.5:1_compression | 6/6_decode | model_portable,
    cross_model      : gemini_cold_read -> 5_misframings_caught,
    boot_file        : governance_compressed -> single_file(AGENTS.md)
  }
  hypotheses_under_test := {
    compressed_governance.survives(new_operating_layers),
    notation.carries_meaning(!pattern_matching),
    cross_model_validation.reveals(framing_errors)
  }
  status := encouraging | !proven | stress_test_ongoing

-- What I learned
FINDING build_reflect :=
  agentic_systems.build(!reflect) UNTIL human.schedules_reflection
  data := 25_arcs | 847_commits | spearman_rho=-0.63
  late_phase := 17.8x_more_narrative_per_commit
  mirrors := {build_reflect_alternation, stop_the_line, maker_manager_tension}
  novel   := ratios >> human_teams_literature

FINDING governance :=
  frameworks(probabilistic_systems) := designable & testable & honestly_assessable
  data := 315_SDs | two_phases | caught_failing | documented_failure
  value := honest_assessment(works & breaks) >> framework_itself

FINDING instances :=
  18+_documented := !victories | learning_process
  each := taught(failure_origin_in_stack, detection_difficulty, control_options)
  taxonomy + layer_model := accumulated_lessons !designed_upfront

-- What I'm looking for
SEEKING := role(sustained_eval, real_conditions) := the_work !side_project
BRING   := {350+_hrs_field_data, named_failure_taxonomy, governance_honestly_assessed, verification_discipline}
PROOF   := process !product | both_repos.public | open_for_scrutiny

LINKS := {oceanheart.ai, github/rickhallett/thepit, github/rickhallett/thepit-v2}
```
