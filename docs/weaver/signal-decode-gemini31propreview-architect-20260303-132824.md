# Signal Decode — gemini31propreview / architect

## Answers

1. The primary objective is getting hired by proving capabilities rather than just claiming them ("hired = proof > claim"). Telling the truth takes priority over getting hired ("truth >> hiring").
2. This standing order means that historical data must be treated as strictly immutable and cannot be modified once recorded.
3. The foot gun occurs when human creativity (L12) and agent sycophancy (L9) combine into a dangerous positive feedback loop. It is stopped by applying a "bearing check" against the system's primary objective (NORTH).
4. A polecat is defined as `claude_p.agents` or a one-shot process that is strictly non-interactive.
5. Prime context is the minimum context required to keep the "smart zone" enabled. Without it, or with stale context, the system enters a "dumb zone" where it produces valid syntax without correct semantics.
6. `SLOP.clear` contradicts verifiable state and is easily detectable in O(1) time. `SLOP.subtle` is consistent with a plausible state but does not match the actual state, requiring O(n) time to detect.
7. L9 warns that accumulated output creates a self-reinforcing loop in the thread position. This loop specifically leads to anchoring, sycophancy, or acquiescence.
8. The notation alone does not define the underlying meanings of terms like `smart_zone`, `claude_p.agents`, or `verifiable_state`. It also lacks the specific contents of the back-referenced decisions and the implementation details of functions like `bearing_check`.
