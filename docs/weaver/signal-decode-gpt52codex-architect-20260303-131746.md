# Signal Decode — gpt52codex / architect

## Answers

1. The primary objective is "hired = proof > claim." Truth overrides hiring, so truth takes priority even if it conflicts with getting hired.
2. It means historical data must never be rewritten; once recorded, it stays immutable.
3. The failure is a positive feedback loop between L12 creativity and L9 sycophancy; the brake is a bearing check against NORTH.
4. A polecat is a `claude -p` agent that is one-shot and non-interactive.
5. Prime context is the minimum context that enables the smart zone; without it (or with stale context), you get valid syntax but broken semantics.
6. SLOP.clear contradicts verifiable state and is easy to detect; SLOP.subtle matches a plausible state but not the actual state and is harder to detect.
7. L9 warns about accumulated output creating self-reinforcing loops via anchoring, sycophancy, and acquiescence.
8. You cannot determine concrete operational details (exact SD content, gate commands, or actual project state) because those references and specifics are missing from the notation.
